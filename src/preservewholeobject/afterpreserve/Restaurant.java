package preservewholeobject.afterpreserve;

public class Restaurant {
    public boolean isWithinDeliveryRange(Address address) {
        // logic to determine if the address is within the delivery range of the restaurant
        return true;
    }
}
